pipeline {
    agent { label 'insights' }
    options {
        timestamps()
    }
    stages {
        stage('Initial Setup') {
            steps {
                sh 'echo Placeholder Jenkinsfile'
            }
        }
    }
}
