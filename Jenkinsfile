pipeline {
    agent { label 'insights' }
    options {
        timestamps()
    }
    stages {
        stage('Initial Setup') {
            step {
                sh 'echo Placeholder Jenkinsfile'
            }
        }
    }
}
