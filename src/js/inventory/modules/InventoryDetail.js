import React from 'react';
import AsyncInventory from './AsyncInventory';

const InventoryDetail = (props) => <AsyncInventory componentName="InventoryDetail" {...props} />;

export default InventoryDetail;
