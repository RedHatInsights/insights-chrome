import React from 'react';
import AsyncInventory from './AsyncInventory';

const TagWithDialog = (props) => <AsyncInventory componentName="TagWithDialog" {...props} />;

export default TagWithDialog;
