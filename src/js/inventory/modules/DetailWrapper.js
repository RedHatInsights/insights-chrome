import React from 'react';
import AsyncInventory from './AsyncInventory';

const DetailWrapper = (props) => <AsyncInventory componentName="DetailWrapper" {...props} />;

export default DetailWrapper;
