import { ScalprumComponentProps } from '@scalprum/react-core';

export const differentFileProps: ScalprumComponentProps = {
  scope: 'foo',
  module: './Bar',
  importName: 'bla',
  fallback: null,
};
